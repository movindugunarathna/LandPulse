import React from "react";

const Loading = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <div className="w-16 h-16 border-t-4 border-custom-green-100 border-solid rounded-full animate-spin"></div>
        </div>
    );
};

export default Loading;
